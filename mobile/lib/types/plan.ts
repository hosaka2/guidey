import type { components } from "@/lib/api/schema";

/** BE の PlanStepResponse をそのまま利用 (openapi-typescript 自動生成)。 */
export type PlanStep = components["schemas"]["PlanStepResponse"];

/** BE の PlanResponse をそのまま利用。 */
export type Plan = components["schemas"]["PlanResponse"];
